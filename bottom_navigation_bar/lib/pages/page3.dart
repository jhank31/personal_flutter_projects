import 'package:flutter/material.dart';

class Page3Page extends StatelessWidget {
   
  const Page3Page({key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('Page3Page'),
      ),
    );
  }
}